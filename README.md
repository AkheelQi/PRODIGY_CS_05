This script captures network packets and prints relevant 
information such as source and destination IP addresses, 
protocols (TCP/UDP), ports, and payload data.
Install scapy library:
pip install scapy
